# Power Set

**Difficulty**: Medium

## Problem Statement

Given a string `s`, return all possible **subsequences** of the string in lexicographical order.

A subsequence is a sequence that can be derived from another sequence by deleting some or no elements without changing the order of the remaining elements.

## Examples

### Example 1:

```
Input: s = "abc"
Output: ["", "a", "ab", "abc", "ac", "b", "bc", "c"]
```

## Constraints

- `1 <= s.length <= 16`
- `s` consists of lowercase English letters.

## Source

[TakeUForward - Power Set](https://takeuforward.org/data-structure/power-set-print-all-the-possible-subsequences-of-the-string/)
